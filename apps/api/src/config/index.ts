import AppConfig from "./app.config";
import AuthConfig from "./auth.config";
import ClientConfig from "./client.config";
import DBConfig from "./db.config";

export default [AppConfig, AuthConfig, ClientConfig, DBConfig];
